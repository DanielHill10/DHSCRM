@model IEnumerable<DHSCRM.Models.JobHeader>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer.CustomerName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-controller="Customers" asp-action="Details" asp-route-id="@item.Customer.CustomerId">@Html.DisplayFor(modelItem => item.Customer.CustomerName)</a>
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.JobHeaderId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JobHeaderId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JobHeaderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
